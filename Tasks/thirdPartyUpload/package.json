{
  "name": "thirdPartyUpload",
  "version": "0.0.3",
  "description": "Upload a third-party scan to the Finite State Platform for analysis.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tfx-cli extension create --manifest-globs ../vss-extension.json --rev-version"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "azure-pipelines-task-lib": "^4.4.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
